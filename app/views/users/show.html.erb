<h2>Feed:</h2>

<% @feeds.each do |feed| %>
  <%= render feed.type.downcase, feed: feed %>
<% end %>

<hr>

<%= form_for @tl_text do |f| %>
  <div class="field">
    <%= f.label :post%><br />
    <%= f.text_area :content %>
  </div>
  <%= f.submit "Submit" %>
<% end %>
