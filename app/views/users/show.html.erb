<h2>Feed:</h2>

<% @feeds.each do |feed| %>
  <%= render feed.type, feed: feed %>
<% end %>

<hr>

<%= form_for @feed_item do |f| %>
  <div class="field">
    <%= f.label :post%><br />
    <%= f.text_area :tl_text %>
  </div>
  <%= f.submit "Submit" %>
<% end %>
